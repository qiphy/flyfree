// Template — GitHub Actions overwrites this with real values on deploy.
// For local dev, fill in your Supabase credentials below.
const FLYFREE_CONFIG = {
    SUPABASE_URL: '',
    SUPABASE_ANON_KEY: '',
};
